{
  "name": "slack-relay",
  "version": "1.0.0",
  "description": "Token authenticated Slack relay server for non-HTTPS compatible devices",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "TheConnMan",
  "license": "MIT",
  "dependencies": {
    "express": "^4.13.4",
    "node-slack": "0.0.7"
  }
}
